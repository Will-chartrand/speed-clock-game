export const setClock = function(clockName) {
      // Set the clock image to the specified clock image
    document.querySelector("#clockBody").setAttribute('src', clockName); // Set the clock name to string argument given
}
